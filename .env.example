# Copy this file to .env and fill in the values, then run `source .env` prior to using Makefile targets.
# Secrets such as LOGIN_PASSWORD or ACCESS_TOKEN should only be stored here if your environment is secure.

# Authentication (choose one of the following)
ACCESS_TOKEN=
LOGIN_PHONE=
LOGIN_PASSWORD=
LOGIN_PASSWORD_MD5=0  # set to 1 if LOGIN_PASSWORD already contains MD5 hash
TOKEN_CACHE=

# Course selection
GROUP_ID=
PACKAGE_ID=
PACKAGE_SEARCH=
ALL_PACKAGES=0
PACKAGE_LIMIT=
COURSE_ID=
XFILE_ID=
TASK_IDS=
DOWNLOAD=0

# Output / runtime behavior
OUTPUT_DIR=downloads
WORKERS=1
MAX_TASKS=

# Listing helpers
LIST_GROUPS=0
LIST_PACKAGES=0
LIST_TASKS=0
LIST_FILES=0

# History recordings
HISTORY_FROM=
HISTORY_TO=
HISTORY_OUTPUT=
